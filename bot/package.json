{
    "scripts": {
        "build": "esbuild --bundle --outdir=dist --external:puppeteer --external:discord.js --external:dotenv --format=esm --platform=node src/main.ts src/lang/*",
        "start": "node dist/main.js"
    },
    "type": "module",
    "dependencies": {
        "dotenv":"16.0.3",
        "discord.js":"14.6.0",
        "puppeteer": "18.2.1",
        "esbuild": "0.15.10"
    }
}