{
    "scripts": {
        "build": "esbuild --bundle --outdir=dist --external:puppeteer --external:discord.js --external:dotenv --format=esm --platform=node src/main.ts",
        "start": "node dist/main.js"
    },
    "type": "module",
    "dependencies": {
        "dotenv":"16.0.3",
        "discord.js":"14.5.0",
        "puppeteer": "18.0.5",
        "esbuild": "0.15.10",
        "concurrently": "7.4.0",
        "nodemon": "2.0.20"
    }
}